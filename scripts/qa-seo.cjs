const fs=require('fs'),assert=require('node:assert/strict');const html=fs.readFileSync('index.html','utf8');const schema=JSON.parse(html.match(/<script type="application\/ld\+json">([\s\S]*?)<\/script>/)[1]);assert.equal(schema['@type'],'School');assert.equal(schema.telephone,'+73466290027');assert.equal(schema.url,'https://evsavelev.github.io/teachers-nizhnevartovsk/');assert.equal(schema.address.streetAddress,'ул. Мира, 93, 1 этаж');assert.ok(!schema.aggregateRating&&!schema.openingHours);assert.equal(new Set([...html.matchAll(/\bid="([^"]+)"/g)].map(m=>m[1])).size,[...html.matchAll(/\bid="([^"]+)"/g)].length);assert.ok(!fs.existsSync('.github/workflows'));console.log('SEO: valid School JSON-LD; correct URL, address, telephone; no invented schema properties; IDs unique; no workflows');
